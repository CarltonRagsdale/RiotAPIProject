using System;
using MingweiSamuel.Camille;
using MingweiSamuel.Camille.Enums;

namespace RiotAPIdemo
{
    class Program
    {
        static void Main()
        {
            /*
            string userinput;

            SummonerV4 summonerApi = new SummonerV4();
            Console.WriteLine("Enter a Leauge of Legends username: ");
            userinput = Console.ReadLine();
            Console.WriteLine("Enter champion Id");
            string championI = Console.ReadLine();
            long championId = Convert.ToInt64(championI);

            var response = summonerApi.GetSummonerByName($"{userinput}");
            var response2 = summonerApi.GetChampionMastery(response.ID, championId);
            Console.WriteLine($"------From Riot's Servers-------\nName: {response.Name} \nLevel: {response.SummonerLevel} \nChampionLevel: {response2.ChampionLevel}");
            */
        }
    }
}
